services:
  geca-api:
    build: .
    container_name: geca-api
    restart: always
    ports:
      - "5000:5000"
    volumes:
      - ./verification.db:/app/verification.db
      - ./.env:/app/.env
    environment:
      - TZ=Europe/Paris
